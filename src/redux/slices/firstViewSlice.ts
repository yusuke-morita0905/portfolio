import { createAsyncThunk, createSlice, PayloadAction, SerializedError } from '@reduxjs/toolkit';

interface State {};

const initialState: State = {};

export const firstViewSlice = createSlice({
  name: 'firstView',
  initialState,
  reducers: {},
});
