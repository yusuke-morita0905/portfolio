import React from 'react';
import styled, { css } from 'styled-components';

type Props = {};

export const ContactFormBodyComp: React.FC<Props> = (p) => {
  return (
    <Body>
      
    </Body>
  );
};

const Body = styled.div`

`;
